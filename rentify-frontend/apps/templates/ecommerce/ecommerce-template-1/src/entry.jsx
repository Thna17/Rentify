/**
 * Template 1 as loaded by the single storefront app (apps/storefront): the
 * template's own stylesheet and app. The storefront provides the Redux store.
 */
import './styles.css';

export { default } from './app';

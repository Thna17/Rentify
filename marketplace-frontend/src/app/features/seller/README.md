# Seller features

Seller-only operational functionality.

```text
seller/
└── orders/  # /seller/orders — incoming order management
```

Seller sign-in lives under `../authentication/seller`. Add future dashboard,
products, store settings, and reports as sibling capability folders here.

# Seller components

Place reusable seller-dashboard components here, grouped by capability:

```text
seller/
├── dashboard/
├── orders/
├── products/
└── store-settings/
```

There are currently no components used exclusively by multiple seller screens;
the seller order screen uses shared layout and order-status components.

# Shared components

Components used across roles or across multiple feature domains.

```text
shared/
├── authentication/
│   └── auth-layout/         # Buyer, seller and admin authentication shell
├── layout/
│   ├── footer/              # Marketplace footer
│   └── navbar/              # Global navigation
├── orders/
│   └── order-status-badge/  # Buyer and seller order status presentation
└── ui/
    └── icon/                # Application SVG icon primitive
```

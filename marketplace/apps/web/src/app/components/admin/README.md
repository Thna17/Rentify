# Admin components

Place reusable administrator components here, grouped by capability:

```text
admin/
├── dashboard/
├── users/
├── moderation/
└── reports/
```

There are currently no reusable admin-only components. The admin login uses the
shared authentication layout.
